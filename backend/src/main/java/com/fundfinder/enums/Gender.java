package com.fundfinder.enums;

/**
 * ANY is only ever used as Scholarship.requiredGender ("open to everyone"),
 * never as a Profile's own gender.
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER,
    ANY
}
